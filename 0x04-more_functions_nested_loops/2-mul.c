/**
 * mul - returns the product of two input values
 * @a: The first argument
 * @b: The second argument
 * Return: Returns the product of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
